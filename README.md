# AI Chat Bot

Универсальный чат-бот на Python с поддержкой двух провайдеров AI: **OpenAI** (GPT-3.5-turbo) и **Anthropic** (Claude Sonnet 4.5) для генерации текста в режиме диалога с сохранением контекста.

## Особенности

- 🤖 **Два AI провайдера**: OpenAI (GPT-3.5-turbo) и Anthropic (Claude Sonnet 4.5)
- 🧠 **Режим размышлений**: Claude Sonnet показывает процесс мышления перед ответом
- 💬 **Сохранение контекста**: Диалог с памятью предыдущих сообщений
- 🔄 **Переключение моделей**: Можно менять модель прямо в диалоге командами
- 🔐 **Безопасность**: API ключ хранится в переменных окружения
- 📊 **Информация о запросе**: После каждого ответа показывается статистика (модель, температура, max tokens, использованные токены)
- 🎨 **Удобный интерфейс**: Консольные команды для управления чатом

## Структура проекта

```
Agent/
├── chat_ai.py           # Основной файл с классом чат-бота
├── test_API.py          # Тестовый файл для проверки API Anthropic
├── requirements.txt     # Зависимости проекта
├── .env                 # Переменные окружения (не коммитить в git!)
├── .gitignore          # Список игнорируемых файлов для Git
├── env_example.txt     # Пример файла .env
├── README.md           # Документация проекта
└── venv/               # Виртуальное окружение Python
```

## Установка

1. Создайте виртуальное окружение:
```bash
python -m venv venv
```

2. Активируйте виртуальное окружение:
```bash
# macOS/Linux
source venv/bin/activate

# Windows
venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` и добавьте ваш API ключ (единый для OpenAI/Anthropic):
```
AI_API_KEY=ваш_api_ключ_здесь
```

## Доступные модели

### OpenAI (режим по умолчанию)
- **gpt-3.5-turbo** - быстрая и экономичная модель для повседневных задач
- Хорошо подходит для общения, генерации текста, базовых вопросов

### Anthropic (думающая модель)
- **claude-sonnet-4-5-20250929** - продвинутая модель с режимом размышлений
- Показывает процесс мышления перед ответом
- Улучшенное логическое мышление и решение сложных задач
- Более глубокий анализ и точные ответы

### Как переключить модель?
Выберите режим при запуске или используйте команды в диалоге:
- `switch openai` - переключиться на OpenAI
- `switch claude` - переключиться на Claude

## Использование

### Запуск чат-бота

**Правильный способ (с активацией venv):**
```bash
# Активация виртуального окружения
source venv/bin/activate

# Запуск приложения
python chat_ai.py
```

**Альтернативный способ (без активации venv):**
```bash
./venv/bin/python chat_ai.py
```

**❌ Неправильно (приведет к ошибке импорта):**
```bash
python3 chat_ai.py  # Без активации venv!
```

При запуске выберите режим:
- **1** - Обычная модель OpenAI (gpt-3.5-turbo) [по умолчанию]
- **2** - Думающая модель Anthropic (claude-sonnet-4-5-20250929)

После выбора модели вам будут предложены параметры настройки:
- **Запрос для модели** - ваше первое сообщение
- **Температура** (0.0-1.0) - креативность ответов
- **Max tokens** - максимальная длина ответа
- **System message** (опционально) - роль/поведение AI

### Статистика запросов
После каждого ответа AI автоматически выводится информация о запросе:
```
============================================================
📊 ИНФОРМАЦИЯ О ЗАПРОСЕ:
   Модель: claude-sonnet-4-5-20250929
   Температура: 0.7
   Max tokens: 1000
   Использовано токенов: 245
============================================================
```

### Команды в диалоге

- `exit` - выход из программы
- `switch claude` - переключиться на Claude (думающая модель)
- `switch openai` - переключиться на OpenAI (обычная модель)
- `model info` - показать информацию о текущей модели
- `clear` - очистить историю диалога

### Пример работы

```
Вы: Привет!
AI: Привет! Чем могу помочь?

Вы: Какая ты модель?
AI: Я - GPT-3.5, языковая модель от OpenAI...

Вы: switch claude
Переключено на Claude. История сброшена.

Вы: Реши задачу: 2+2*2
AI: Размышления: Пользователь задал математическую задачу...

Ответ: Согласно порядку операций, сначала выполняется умножение...
```

## Требования

- Python 3.8+
- Модули: `openai`, `anthropic`, `python-dotenv` (устанавливаются из requirements.txt)
- API ключ OpenAI или Anthropic (или оба для полного функционала)

## API класса ChatAI

Класс `ChatAI` предоставляет следующие возможности:

### Методы
- `send_message(message, system_prompt=None)` - отправка сообщения и получение ответа с сохранением контекста
- `set_system_prompt(prompt)` - установка системного промпта (роли AI)
- `clear_history()` - очистка истории сообщений
- `get_history()` - получение истории разговора в виде списка

### Атрибуты
- `provider` - текущий провайдер AI ("openai" или "anthropic")
- `model` - используемая модель
- `messages` - история сообщений
- `last_thinking_text` - последние размышления Claude (если есть)

### Пример программного использования

```python
from chat_ai import ChatAI

# Создаём чат-бот с OpenAI
chat = ChatAI(provider="openai", model="gpt-3.5-turbo")

# Устанавливаем роль
chat.set_system_prompt("Ты - опытный программист Python")

# Отправляем сообщение
response = chat.send_message("Как работает декоратор?")
print(response)

# Переключаемся на Claude
chat = ChatAI(provider="anthropic", model="claude-sonnet-4-5-20250929")
response = chat.send_message("Объясни квантовую запутанность")

# Проверяем размышления
if chat.last_thinking_text:
    print("Размышления:", chat.last_thinking_text)
print("Ответ:", response)
```

## Возможности для развития

Этот проект можно расширить множеством способов:

### 📋 Нагружать различными задачами
- Добавить специализированные режимы (переводчик, копирайтер, программист)
- Создать шаблоны системных промптов для разных задач
- Реализовать сохранение пресетов для быстрого переключения ролей

### 📄 Загружать текстовые файлы для преобразования
- Добавить команду для загрузки `.txt`, `.md`, `.docx` файлов
- Реализовать анализ документов (резюмирование, извлечение ключевых слов)
- Поддержка пакетной обработки файлов
- Экспорт результатов в различные форматы

### 💻 Общаться с нейросетью прямо в консоли
- ✅ **Уже реализовано!** Интерактивный консольный интерфейс с настройкой параметров
- Добавить подсветку синтаксиса для кода
- Реализовать историю команд (стрелки вверх/вниз)
- Автодополнение команд

### 🤖 Подключать к Telegram-боту
- Интегрировать с Telegram Bot API
- Каждый пользователь получает свою историю диалога
- Команды бота для переключения моделей
- Возможность отправки файлов боту для анализа

### 🎯 Создать собственного личного бота
- Настроить уникальный характер и стиль общения
- Обучить бота на собственных данных
- Добавить интеграцию с вашими сервисами (календарь, почта, задачи)
- Голосовое управление через распознавание речи

### 🔧 Дополнительные идеи
- Веб-интерфейс на Flask/FastAPI
- Сохранение истории диалогов в базу данных
- Мультимодальность (работа с изображениями для Claude)
- RAG (Retrieval-Augmented Generation) для работы с базой знаний
- Streaming режим для постепенного вывода ответа

## Важные замечания

⚠️ **Безопасность API ключей**
- Никогда не публикуйте файл `.env` в публичных репозиториях
- Используйте разные ключи для разработки и продакшена
- Регулярно обновляйте и ротируйте API ключи

💡 **Советы по использованию**
- Claude лучше справляется с аналитическими и логическими задачами
- GPT-3.5 быстрее и дешевле для простых запросов
- Используйте системные промпты для настройки поведения AI
- Очищайте историю (`clear`) при смене темы разговора

🔧 **Устранение проблем**
- **Ошибка "ModuleNotFoundError"**: активируйте виртуальное окружение `source venv/bin/activate`
- **Ошибка "EOFError"**: не запускайте в автоматическом режиме, требуются пользовательские вводы
- Если модель не отвечает, проверьте API ключ в `.env`
- Убедитесь, что активировано виртуальное окружение перед запуском
- Проверьте подключение к интернету
- Посмотрите детали ошибки в консоли

## 📝 Лицензия

MIT License - свободно используйте в учебных и коммерческих целях.

## Вклад

Буду рад вашим предложениям по улучшению! Создавайте issues и pull requests.

---

**Приятного использования! 🚀**

*Создано с помощью OpenAI и Anthropic AI*
